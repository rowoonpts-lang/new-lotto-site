4개의 정보가 묶음으로 이루어지고, 여러번 입력을 할 수 있습니다.

같은 bind_address 에 같은 bind_id를 사용할 수 없습니다.

bind_address=
bind_port=3000
bind_id=발급받은 문자발송 모듈의 아이디
bind_pwd=발급받은 문자발송 모듈의 암호

bind_address, bind_id, bind_pwd는 발급 받아 사용을 합니다.

